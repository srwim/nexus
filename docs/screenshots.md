# Screenshots

Drop images in this folder and reference them from the README.

| File | What to capture |
| --- | --- |
| `docs/blocked-deployment.png` | The `send` job blocked on the `digest-send` environment, awaiting review. Actions → a daily newsletter run → the yellow "Review pending deployments" banner with the `build-brief` job green above it. |
| `docs/eval-run.png` | *(optional)* `npm run evals` output — the per-case table and aggregate row. |

The first one is the point: it is the autonomy gate visibly holding a real send.
